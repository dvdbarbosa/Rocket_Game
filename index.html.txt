<!doctype html>
<html lang="pt-br">
  <head>
    <meta charset="utf-8" />
    <title>Jogo de Nave</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Ruffle (emulador de Flash) -->
    <script src="https://unpkg.com/@ruffle-rs/ruffle"></script>

    <style>
      html, body {
        height: 100%; margin: 0; background: #000; color: #fff; font-family: system-ui, sans-serif;
        display: grid; place-items: center;
      }
      .frame {
        width: 800px;   /* ← AJUSTE para a LARGURA do seu Stage */
        height: 600px;  /* ← AJUSTE para a ALTURA do seu Stage  */
        box-shadow: 0 0 40px rgba(0,0,0,0.6);
        border: 1px solid #222;
        background: #000;
      }
      .hint { margin-top: 10px; opacity: .8; font-size: 14px; text-align: center; }
    </style>
  </head>
  <body>
    <div class="frame" id="game-container"></div>
    <div class="hint">Clique no jogo para focar e usar o teclado (Enter, setas, espaço, P…)</div>

    <script>
      // Cria e injeta o player do Ruffle na div .frame
      window.addEventListener("DOMContentLoaded", () => {
        const ruffle = window.RufflePlayer.newest();
        const container = document.getElementById("game-container");
        const player = ruffle.createPlayer();
        container.appendChild(player);
        player.style.width = "100%";
        player.style.height = "100%";
        // Carrega seu SWF que está na mesma pasta
        player.load("game.swf");

        // Garante foco ao clicar (teclado funciona)
        container.tabIndex = 0;
        container.addEventListener("click", () => container.focus());
      });
    </script>
  </body>
</html>
